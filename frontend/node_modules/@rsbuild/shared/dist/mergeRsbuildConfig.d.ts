/**
 * When merging config, some properties prefer `override` rather than `merge to array`
 */
export declare const isOverriddenConfigKey: (key: string) => boolean;
export declare const mergeRsbuildConfig: <T>(...configs: T[]) => T;
